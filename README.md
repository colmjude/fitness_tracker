Make it visible. Make it public. Make me accountable. Make me fitter?

###Dependencies

Requires `node` packages `forEachAsync` and `date-utils`. Both can be installed with npm. E.g.

    npm install date-utils

